# 미니수첩 사용설명서 (v0.6 테스트판)

디지털 미니 수첩: 이미지 노트를 여백 없는 격자로 한눈에 보고, 누르면 바로 편집하고, 좌우로 밀어 다음 노트로 넘어가는 앱.
PC(윈도우)와 안드로이드에서 같은 코드로 돌아가는 웹앱(PWA)입니다. 설치할 프로그램은 없어요.

---

## 1. PC에서 바로 쓰기

1. `미니수첩 실행.bat` 더블클릭 → Edge 앱 창으로 열립니다.
2. **PC 폴더 열기** → 노트 이미지를 둘 폴더를 선택합니다.
   - 추천: 구글 드라이브 동기화 폴더 안에 노트 폴더를 하나 만드세요.
     예) `G:\내 드라이브\미니수첩노트`
   - 이렇게 하면 폰에서 같은 폴더를 Google Drive로 열 수 있어요.
3. (선택) Edge 주소창 오른쪽 `…` → **앱** → **이 사이트를 앱으로 설치**를 누르면 시작 메뉴와 작업 표시줄에 고정할 수 있어요.
   - 설치한 뒤에도 `미니수첩 실행.bat`으로 로컬 서버를 먼저 켜야 합니다. 3단계에서 인터넷 주소에 올리면 이 과정이 필요 없어요.

> 처음 폴더를 열면 브라우저가 "파일 편집 허용"을 물어요. **허용**을 누르세요. 다음 실행부터는 "이어서 열기"만 누르면 됩니다.

## 2. 저장 방식 (핵심) — 여러 폴더 + 앱 폴더 하나

```
첫 번째 폴더/ (예: 메모)
├─ 메모1.png                       ← 원본 이미지. 저장하면 자동으로 덮어씀
├─ _편집파일/                       ← "앱 폴더": 모든 폴더의 편집파일이 여기 모임
│   ├─ 메모__메모1.png.mnote
│   └─ 일기__0924.png.mnote         ← 다른 폴더(일기)의 이미지 편집파일도 여기
└─ _휴지통/
다른 폴더/ (예: 일기)
└─ 0924.png                        ← 저장하면 이 폴더에서 바로 교체돼요
```

- **폴더 추가**: 갤러리 위쪽 폴더 줄 끝의 "폴더 추가"를 누르면 다른 폴더의 이미지도 함께 보고 편집할 수 있어요. "전체"는 모든 폴더를 합쳐서, 폴더 이름을 누르면 그 폴더만 보여줘요.
- 폴더 이름을 길게 누르면(오른쪽 클릭) **"새 노트를 이 폴더에 저장"**, **"목록에서 빼기"**(파일은 그대로)를 고를 수 있어요.
- 이미지를 편집하고 **저장**하면 원본 이미지는 원래 폴더에서 바뀌고, 레이어가 살아있는 편집파일은 **앱 폴더** 한 곳에 모여요. 앱 폴더는 설정 → 저장 위치에서 바꿀 수 있어요(기본: 첫 번째 폴더 안의 `_편집파일`).
- 이름이 같은 폴더 두 개는 함께 추가할 수 없어요(편집파일 이름이 겹치지 않게).
- PC와 폰에서 같은 폴더들을 추가하고 같은 앱 폴더를 쓰면 편집파일도 이어져요.
- 다음에 같은 이미지를 열면 편집파일이 자동으로 열려서 레이어가 그대로 살아 있어요.
- 다른 앱에서 원본 이미지를 고쳤다면, 열 때 **"원본을 새 레이어로 추가 / 원본으로 새로 시작 / 편집파일 그대로"** 중에서 고를 수 있어요.
- 노트를 넘기거나 갤러리로 돌아갈 때 **뒤에서 자동 저장**돼요(기다리지 않아요). 다음·이전 노트는 미리 불러둬서 바로 넘어가요.
- 갤러리 타일 오른쪽 아래의 레이어 아이콘은 편집파일이 있다는 표시예요.

### 이미지 가져오기
- **이미지 넣기**: 편집 화면 위쪽의 그림 아이콘 → 사진/카메라/파일에서 골라 새 레이어로 넣어요(바로 크기·위치 조절).
- **끌어다 놓기**: 다른 창·앱·캡처 팝업에서 이미지를 끌어다 놓으면 편집 중이면 새 레이어로, 갤러리에서는 새 노트로 들어가요.
- **공유하기(안드로이드)**: 갤러리 앱 등에서 공유 → **미니수첩**을 고르면 새 노트로 열려요. (앱을 설치한 뒤 처음이라면 공유 목록에 뜨기까지 시간이 걸리거나, 앱을 지웠다 다시 설치해야 할 수 있어요.)
- PC에서는 Ctrl+V 붙여넣기도 돼요.

## 3. 조작법

### 갤러리
| 동작 | PC | 폰 |
|---|---|---|
| 노트 편집 | 클릭 | 탭 |
| 메뉴 (이름 바꾸기/휴지통/정보) | 오른쪽 클릭 | 길게 누르기 |
| 열 수 바꾸기 | Ctrl + 휠 | 두 손가락 오므리기/벌리기 |
| 새 노트 | 오른쪽 아래 + | 오른쪽 아래 + |

### 편집 화면
| 기능 | 단축키 / 제스처 |
|---|---|
| 브러시 / 지우개 / 채우기 | B / E / G |
| 사각 선택 / 올가미 / 변형 | M / L / V (Ctrl+T) |
| 스포이트 | I, Alt+클릭, 마우스 오른쪽 클릭 |
| 텍스트 | T — 빈 곳을 누르면 새 글상자, 글상자를 누르면 수정, 끌면 이동, Ctrl+Enter = 확정 |
| 손(화면 이동) | H, Space+드래그, 휠 클릭 드래그 / 두 손가락 |
| 확대·축소 | 마우스 휠, Ctrl +/- / 두 손가락 오므리기 |
| 캔버스 회전 | Q / W (15°씩), R = 초기화 / 두 손가락 돌리기 (90° 근처에서 딱 맞춰짐), 오른쪽 아래 "↻" 버튼 = 초기화 |
| 화면에 맞추기 | Ctrl+0 |
| 브러시 목록 바로가기 | 1~9 (연필·펜·형광펜·즐겨찾기 순) |
| 레이어 | 새 레이어 Ctrl+Shift+N, 복제 Ctrl+J, 병합 Ctrl+E, 위/아래로 옮기기 Ctrl+] / Ctrl+[, 위/아래 레이어 선택 Alt+] / Alt+[ |
| 브러시 크기 | [ / ], 왼쪽 세로 슬라이더(위: 크기, 아래: 불투명도) |
| 실행 취소 / 다시 실행 | Ctrl+Z / Ctrl+Y · 두 손가락 탭 / 세 손가락 탭 |
| 저장 | Ctrl+S |
| 이전 / 다음 노트 | ← / → · 한 손가락으로 좌우 스와이프 (손가락 그리기가 꺼져 있을 때) |
| 선택: 전체 / 해제 / 반전 | Ctrl+A / Ctrl+D / Ctrl+Shift+I |
| 선택 영역 지우기 | Delete |
| 복사 / 잘라내기 / 붙여넣기 | Ctrl+C / X / V (다른 앱에서 복사한 이미지도 새 레이어로 붙여넣기) |
| 변형 확정 / 취소 | Enter / Esc |

**단축키는 전부 바꿀 수 있어요**: 설정 → 기타 → "단축키 설정". 한글 입력 상태에서도 같은 키로 동작해요.

**펜과 손가락**: S펜이나 와콤 펜을 한 번 쓰면 그 뒤로 손가락은 그리지 않고 이동·확대·스와이프 전용이 됩니다(손바닥 오인식 방지). 설정에서 바꿀 수 있어요.

**브러시**: 연필(크림 연필 스타일: 가운데는 진하고 촘촘, 가장자리와 약한 필압은 거칠게), 펜, 형광펜. 브러시 탭에서 크기·불투명도·흐름·경도·연필 질감·손떨림 보정을 조절합니다.

**형광펜**: 끝 모양(둥글게/각지게), 겹칠 때(덮기/형광펜: 같은 레이어의 글씨가 비쳐 보임)를 브러시 탭에서 고를 수 있어요. 긋다가 펜을 떼지 않고 잠깐(0.5초) 멈추면 직선으로 바뀌고, 그 상태로 움직이면 끝점이 따라와요. 이 "멈추면 직선" 기능은 다른 브러시에서도 켤 수 있어요.

**필압**: 브러시 탭 아래쪽에 있어요. "필압 → 크기"와 "최소 크기"(기본 15%: 약하게 누르면 가늘게), "필압 → 농도"(연필: 약하게 누르면 흐리고 거칠게), "곡선"(부드럽게↔단단하게), "필압 → 불투명도". 그래프로 바로 확인할 수 있어요.

**텍스트**: 텍스트 도구(T)로 글상자를 만들어요. 글꼴(기본·고딕·명조·손글씨·둥근·붓글씨), 크기, 굵게를 고를 수 있고 색은 지금 색을 써요. 글상자마다 텍스트 레이어가 따로 생겨서 저장한 뒤에도 다시 고칠 수 있어요. 레이어 테두리를 켜면 테두리 글씨가 돼요. 글꼴은 처음 한 번 인터넷에서 받아오고 그다음부터는 오프라인에서도 써져요.

$1: 브러시 목록 끝의 "＋ 즐겨찾기"를 누르면 지금 브러시 설정과 색이 함께 저장돼요. 누르면 그 브러시와 색으로 바로 바뀌고, 길게 누르거나 오른쪽 클릭하면 이름 바꾸기·지금 색으로 바꾸기·순서 옮기기·삭제를 할 수 있어요.

**테두리 (레이어 속성, 클립스튜디오 "경계 효과"와 같은 방식)**: 레이어 탭 → "테두리 (경계 효과)". 그 레이어에 그려진 모든 것의 바깥 윤곽에 테두리가 생겨요. 테두리를 켜기 전에 그린 선에도 적용되고, 선이 겹쳐도 합쳐진 모양의 바깥에만 둘러져요. 색, 굵기, 매끈함(매끈 = 거친 연필이어도 테두리는 둥글고 매끈, 거칠게 = 선 모양을 그대로 따라감)을 레이어마다 정할 수 있어요. 테두리가 필요한 글씨와 필요 없는 그림은 레이어를 나눠 쓰세요. 이미지를 열면 이미지 위에 빈 "레이어 1"이 자동으로 생겨요.

**필압이 안 될 때 (와콤)**: 브러시 탭의 "필압 테스트" 칸에 펜으로 그어보세요.
- "입력: 마우스 · 필압 0.50"으로 나오면 와콤 드라이버의 **Windows Ink**가 꺼져 있는 거예요(브라우저는 Windows Ink로만 필압을 받아요).
  1. 시작 메뉴에서 **Wacom 태블릿 등록 정보**를 엽니다.
  2. 위쪽 "애플리케이션" 줄에서 **모두**를 선택합니다. (클립스튜디오는 따로 등록돼 있어서 영향 없음)
  3. **펜 → 매핑** 탭에서 **Windows Ink 사용**에 체크합니다.
  4. 미니수첩 창을 닫았다가 다시 엽니다.
- "펜 · 필압 0.xx ✓"로 나오는데 약하게 느껴지면 "최소 크기"를 낮추고(예: 10%) "곡선"을 부드럽게 쪽으로 옮기세요.

**레이어**: 끌어서 순서 바꾸기(마우스·펜은 줄 전체, 손가락은 오른쪽 ≡ 손잡이나 길게 누른 뒤 끌기), 추가, 복제, 아래로 병합, 순서 변경, 삭제, 보이기/숨기기, 불투명도, 합성 모드 17종(곱하기·스크린·오버레이 등), 투명 픽셀 잠금, 이름 변경(더블클릭).

---

## 4. 안드로이드에서 쓰기 (추가 설정 두 가지)

폰에서 쓰려면 ① 앱을 인터넷 주소(https)에 올리고 ② Google Drive 접근 키를 한 번 만들어야 해요.

### ① 앱을 https 주소에 올리기 (무료, GitHub Pages)
1. github.com에 가입 또는 로그인합니다.
2. 새 저장소(repository)를 만듭니다. 예: `mininote`, Public.
3. 이 폴더의 파일을 전부 업로드합니다(`index.html`, `css`, `js`, `icons`, `manifest.webmanifest`, `sw.js`).
4. 저장소 **Settings → Pages → Branch: main / root → Save**를 누릅니다.
5. 1~2분 뒤 `https://<내아이디>.github.io/mininote/` 주소가 생깁니다.
6. 폰 Chrome으로 이 주소를 열고 메뉴 → **홈 화면에 추가(앱 설치)**를 누릅니다.

> 이미 배포됨: https://sureon-hub.github.io/mininote/

### ② Google Drive 연결용 Client ID 만들기 (1회, 약 5분)
1. https://console.cloud.google.com 접속 → 상단에서 **새 프로젝트** 만들기 (이름: mininote).
2. **API 및 서비스 → 라이브러리** → "Google Drive API" 검색 → **사용**.
3. **API 및 서비스 → OAuth 동의 화면**(또는 "Google 인증 플랫폼")
   - 사용자 유형: **외부**, 앱 이름: 미니수첩, 지원 이메일: 내 Gmail
   - **대상(테스트 사용자)**에 내 Gmail 주소를 추가합니다. 게시 상태는 "테스트"로 두면 됩니다.
4. **사용자 인증 정보 → 사용자 인증 정보 만들기 → OAuth 클라이언트 ID**
   - 애플리케이션 유형: **웹 애플리케이션**
   - **승인된 JavaScript 원본**에 두 주소를 추가합니다.
     - `http://localhost:5178` (PC에서 쓸 때)
     - `https://<내아이디>.github.io` (폰에서 쓸 때. 끝에 `/mininote`는 붙이지 않음)
5. 만들어진 **클라이언트 ID**(`…apps.googleusercontent.com`)를 복사해서 앱 **설정 → Google Drive → OAuth 클라이언트 ID**에 붙여넣습니다.
6. 첫 화면에서 **Google Drive 폴더 연결** → 로그인 → 노트 폴더 선택.
   - "Google에서 확인하지 않은 앱" 경고가 나오면 **고급 → 미니수첩(으)로 이동**을 누르세요. 내가 만든 개인용 앱이라서 뜨는 경고예요.
   - 로그인은 약 1시간마다 만료돼요. 만료되면 앱이 다시 로그인 버튼을 보여줘요.

PC(G: 동기화 폴더)와 폰(Google Drive)이 같은 폴더를 보고 있으니, 한쪽에서 저장하면 다른 쪽에도 반영됩니다(드라이브 동기화에 몇 초~몇십 초 걸림).

---

## 5. 알아둘 점 (테스트판 한계)
- 한 노트를 PC와 폰에서 **동시에** 편집하면 나중에 저장한 쪽이 이깁니다.
- 선택 영역을 만들거나 바꾼 것은 실행 취소 기록에 남지 않아요(그리기·레이어 작업은 남음).
- 아주 큰 이미지(2500만 화소 이상)는 폰에서 느릴 수 있어요.
- 지원 형식: PNG, JPG, WEBP. 원래 형식 그대로 저장합니다(JPG는 투명한 부분이 흰색이 됨).
- "앱 내부 저장소로 체험"은 그 기기의 브라우저 안에만 저장되는 테스트 공간이에요.

## 파일 구성
| 파일 | 역할 |
|---|---|
| `index.html`, `css/app.css` | 화면 |
| `js/util.js` | 공통 도구, 설정, 아이콘 |
| `js/storage.js` | 저장소 (PC 폴더 / 앱 내부 / Google Drive) |
| `js/project.js` | 편집파일(.mnote) 형식, 열기·저장·이름 변경·휴지통 |
| `js/gallery.js` | 격자 갤러리 |
| `js/editor/*.js` | 편집기 (문서·레이어, 브러시 엔진, 도구, 패널, 입력) |
| `serve.ps1`, `미니수첩 실행.bat` | PC용 로컬 서버와 실행 파일 |
| `sw.js`, `manifest.webmanifest` | 앱 설치와 오프라인 실행 |
